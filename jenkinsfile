pipeline {
    agent { label 'slave-node​' }

    stages {
        stage('Build') {
            
            steps {
                echo 'Building..'
                sh '''
                    ls -al
                '''
            }
        }
    }
}